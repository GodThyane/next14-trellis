import React from 'react';

interface Props {
   params: {
      id: string;
   };
}

const OrganizationByIdPage = ({ params }: Props) => {
   return <div>Organization page</div>;
};

export default OrganizationByIdPage;
